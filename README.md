# Smart Lawyer Portal - مدخل المحامي الذكي

نظام إدارة شامل للمحامين ومكاتب المحاماة يتضمن مساعد ذكي مدعوم بالذكاء الاصطناعي.

## المميزات / Features

### ✨ المميزات الرئيسية

- 🔐 **نظام مصادقة متكامل** - تسجيل دخول وإنشاء حسابات للمحامين والعملاء
- ⚖️ **إدارة القضايا** - إضافة وتتبع القضايا مع كافة التفاصيل
- 👥 **إدارة العملاء** - قاعدة بيانات متكاملة للعملاء
- 📄 **إدارة المستندات** - رفع وتنظيم المستندات القانونية
- 🤖 **مساعد ذكي AI** - مساعد محادثة ذكي للاستشارات القانونية
- 📊 **لوحة تحكم** - عرض إحصائيات ومعلومات شاملة
- 📱 **تصميم متجاوب** - يعمل على جميع الأجهزة

### 🏗️ البنية التقنية

- **Frontend Framework**: Angular 19
- **Language**: TypeScript
- **Styling**: SCSS with RTL support
- **State Management**: Angular Signals
- **HTTP Client**: Angular HttpClient with Interceptors
- **Routing**: Angular Router with Lazy Loading
- **Forms**: Reactive Forms

## 📁 هيكل المشروع / Project Structure

```
src/
├── app/
│   ├── core/                    # الوظائف الأساسية
│   │   ├── models/             # النماذج والواجهات
│   │   ├── services/           # الخدمات
│   │   ├── guards/             # حراس التوجيه
│   │   └── interceptors/       # معترضات HTTP
│   │
│   ├── features/               # ميزات التطبيق
│   │   ├── auth/              # المصادقة (تسجيل دخول/إنشاء حساب)
│   │   ├── dashboard/         # لوحة التحكم
│   │   ├── cases/             # إدارة القضايا
│   │   ├── clients/           # إدارة العملاء
│   │   ├── documents/         # إدارة المستندات
│   │   └── ai-assistant/      # المساعد الذكي
│   │
│   ├── layouts/               # التخطيطات
│   │   └── main-layout/      # التخطيط الرئيسي مع القائمة الجانبية
│   │
│   └── shared/               # المكونات المشتركة
│
└── environments/             # إعدادات البيئة
```

## 🚀 البدء / Getting Started

### المتطلبات المسبقة

- Node.js (v18 أو أحدث)
- npm أو yarn
- Angular CLI (`npm install -g @angular/cli`)

### التثبيت

```bash
# استنساخ المشروع
git clone <repository-url>

# الانتقال إلى مجلد المشروع
cd smart-lawyer-portal

# تثبيت التبعيات
npm install
```

### تشغيل المشروع

```bash
# تشغيل خادم التطوير
npm start
# أو
ng serve

# افتح المتصفح على
# http://localhost:4200
```

### البناء للإنتاج

```bash
# بناء المشروع
npm run build

# الملفات المبنية ستكون في مجلد dist/
```

## 🔧 الإعدادات / Configuration

### ملفات البيئة

قم بتحديث `src/environments/environment.ts` للتطوير:

```typescript
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',  // عنوان API الخاص بك
  appName: 'Smart Lawyer Portal',
  version: '1.0.0'
};
```

للإنتاج، قم بتحديث `src/environments/environment.prod.ts`:

```typescript
export const environment = {
  production: true,
  apiUrl: 'https://your-api-domain.com/api',
  appName: 'Smart Lawyer Portal',
  version: '1.0.0'
};
```

## 📚 الاستخدام / Usage

### تسجيل الدخول

1. افتح التطبيق في المتصفح
2. سيتم توجيهك تلقائياً لصفحة تسجيل الدخول
3. أدخل بيانات الدخول أو أنشئ حساب جديد

### إضافة قضية جديدة

1. من لوحة التحكم، اضغط على "قضية جديدة"
2. املأ تفاصيل القضية
3. حدد العميل والأولوية
4. احفظ القضية

### استخدام المساعد الذكي

1. اضغط على "المساعد الذكي" من القائمة الجانبية
2. اكتب سؤالك أو استفسارك القانوني
3. استخدم Ctrl + Enter للإرسال
4. سيرد المساعد بالمعلومات المطلوبة

## 🔐 نظام الصلاحيات / Permissions

النظام يدعم أربعة أنواع من المستخدمين:

- **Admin** - صلاحيات كاملة
- **Lawyer** - محامي (إدارة قضايا وعملاء)
- **Client** - عميل (عرض قضاياه فقط)
- **Assistant** - مساعد قانوني (مساعدة في القضايا)

## 🎨 التخصيص / Customization

### تغيير الألوان الأساسية

عدل ملف `src/styles.scss`:

```scss
// الألوان الأساسية
$primary-color: #667eea;
$secondary-color: #764ba2;
$background-color: #f7fafc;
$text-color: #2d3748;
```

### إضافة صفحة جديدة

1. أنشئ مكون جديد:
```bash
ng generate component features/your-feature
```

2. أضف المسار في `app.routes.ts`:
```typescript
{
  path: 'your-feature',
  loadComponent: () => import('./features/your-feature/your-feature.component')
}
```

## 📡 API Integration

النظام يتوقع API backend بالنقاط التالية:

### Authentication
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/register` - إنشاء حساب
- `POST /api/auth/refresh` - تحديث التوكن

### Cases
- `GET /api/cases` - جلب القضايا
- `POST /api/cases` - إضافة قضية
- `GET /api/cases/:id` - جلب قضية محددة
- `PUT /api/cases/:id` - تحديث قضية
- `DELETE /api/cases/:id` - حذف قضية

### Clients
- `GET /api/clients` - جلب العملاء
- `POST /api/clients` - إضافة عميل
- `GET /api/clients/:id` - جلب عميل محدد
- `PUT /api/clients/:id` - تحديث عميل

### Documents
- `GET /api/documents` - جلب المستندات
- `POST /api/documents` - رفع مستند
- `GET /api/documents/:id/download` - تحميل مستند

### AI Assistant
- `POST /api/ai/chat` - إرسال رسالة للمساعد الذكي
- `GET /api/ai/sessions` - جلب جلسات المحادثة

## 🧪 الاختبار / Testing

```bash
# تشغيل الاختبارات
npm test

# تشغيل الاختبارات مع التغطية
npm run test:coverage
```

## 📝 الترخيص / License

هذا المشروع مرخص تحت [MIT License](LICENSE)

## 👥 المساهمة / Contributing

المساهمات مرحب بها! يرجى:

1. Fork المشروع
2. أنشئ فرع للميزة الجديدة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

## 📞 الدعم / Support

للمساعدة أو الاستفسارات:
- افتح Issue في GitHub
- راسلنا على support@smartlawyer.com

## 🗺️ خارطة الطريق / Roadmap

- [ ] إضافة نظام التقارير والإحصائيات
- [ ] دعم المواعيد والتذكيرات
- [ ] تكامل مع التقويم
- [ ] إشعارات الوقت الفعلي
- [ ] تطبيق موبايل
- [ ] دعم متعدد اللغات

---

Built with Angular 19 | Made with ❤️ for the legal community
